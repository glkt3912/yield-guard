version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - unused
    - govet
  settings:
    errcheck:
      check-type-assertions: true

issues:
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - errcheck
