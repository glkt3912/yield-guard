version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - unused
    - govet
  settings:
    errcheck:
      check-type-assertions: true

exclusions:
  rules:
    - path: "_test\\.go"
      linters:
        - errcheck
