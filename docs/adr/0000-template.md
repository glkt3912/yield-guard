---
purpose: ADR のテンプレート（却下欄・降格復活条件を必須にする）
triggers: [adr, template, decision, 決定, 却下]
audience: all
token_weight: small
---

# ADR-XXXX: <決定タイトル>

- **状態**: 提案 | 採用 | 却下 | 置換（→ ADR-YYYY）
- **日付**: YYYY-MM-DD
- **対象ペルソナの問い**: <`docs/product-decision.md` の「問い」を引用する。ここが空なら ADR は書けない>

## 背景

<なぜこの判断が必要になったか。実装の現状を事実で書く（ファイルパス・行数・数値）>

## 決定

<採用した案を1つ。「両方」は決定ではない>

## 却下した案

> **必須**。空欄の ADR は受け付けない。却下は「劣っているから」ではなく
> 「対象ペルソナの問いに答えないから」で書く。

| 案 | 却下理由（ペルソナの問いに照らして） |
|---|---|
| | |

## 両方残す場合

> 両方残すこと自体は禁止しない。禁止するのは**どちらが既定か決めないこと**。
> 降格側の復活条件が空なら、それは決定ではなく先送りである。

| | 実装 | 条件 |
|---|---|---|
| 既定 | | — |
| 降格 | | <これが満たされたら既定に戻す、という具体条件> |

## この判断を覆す条件

<前提が崩れる具体的な事象。「状況が変わったら」は書かない>

## 結果として捨てる能力

<この決定で失うユーザー価値・機能。空欄なら、何も選んでいない可能性がある>
