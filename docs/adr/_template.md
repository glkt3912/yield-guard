---
purpose: ADR のテンプレート（検討した案を先に列挙し、却下理由を必須にする）
triggers: [adr, template, decision, 決定, 却下]
audience: all
token_weight: small
---

<!--
ファイル名は `YYYYMMDD-<slug>.md`（例: 20260910-input-mode.md）。
連番を使わないのは、worktree を並走させたときに採番が衝突するため。
-->

# <決定タイトル>

- **状態**: 提案 | 採用 | 暫定 | 却下 | 置換（→ 置換先のファイル名）
- **日付**: YYYY-MM-DD
- **対象ペルソナの問い**: <`docs/product-decision.md` の「問い」を引用する。
  未記入なら `未決定` と書く。その場合この ADR の状態は **暫定** とし、
  問いが決まった時点で却下理由を書き直す>

## 背景

<なぜこの判断が必要になったか。実装の現状を事実で書く（ファイルパス・行数・数値）>

## 検討した案

> **決定を書く前にここを埋める。** 順序を逆にすると、先に結論を書いてから
> 却下理由を後付けすることになり、この文書の意味が無くなる。
> 採用予定の案も、そうでない案も、ここでは対等に並べる。

| 案 | 内容 | 対象ペルソナの問いへの答え方 |
|---|---|---|
| A | | |
| B | | |

## 決定

<採用した案を1つ>

> 両方残す場合も「決定」はここに1行で書く。
> 専用の節は設けない。**どちらが既定かを書かない限り、それは決定ではなく先送り**である。
> 書く内容: 既定はどちらか / 降格側は何か / 降格側が既定に戻る具体条件。

## 却下した案

> **必須**。空欄の ADR は受け付けない。
> 却下は「劣っているから」ではなく「対象ペルソナの問いに答えないから」で書く。
> 純粋に技術的に劣る場合のみ、その理由をそのまま書いてよい。

| 案 | 却下理由 |
|---|---|
| | |

## この判断を覆す条件

<前提が崩れる具体的な事象。「状況が変わったら」は書かない>

## 結果として捨てる能力

<この決定で失うユーザー価値・機能。空欄なら、何も選んでいない可能性がある>

> 得るものを書く欄は意図的に設けていない。
> `docs/architecture.md` の「技術選定の背景（Why）」が採用理由だけを蓄積し、
> 代替案が明示的に死なない形式になっていたことへの対抗である。
